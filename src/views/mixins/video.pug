mixin video(video)
    h3
        a(href=`/videos/${video.id}`) #{video.title}
    ul 
        li 
            h4=video.description
        li    
            h4=video.hashtag
        li
            h4=video.createdAt
    hr