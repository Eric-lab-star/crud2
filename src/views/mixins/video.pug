mixin video(video)    
    section
        aside
            a(href=`/videos/${video.id}`)=video.title
            ul
                li=video.description
                li=video.createdAt
                li=video.hashtag
                li #{video.view} views
    