extends ../base

block content
    if errorMessage
        span=errorMessage
    form(method="post")
        input(placeholder="Old Password", type="password",required, name="oldPassword")
        input(placeholder="New Password", type="password",required, name="newPassword")
        input(placeholder="New Password Confirmation", type="password",required, name="confirmation")
        input(type="submit", value="Save")